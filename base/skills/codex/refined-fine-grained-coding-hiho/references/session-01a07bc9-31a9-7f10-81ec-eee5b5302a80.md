# 採用値が残ることと、利用先で使われることを確かめる

対象はセッション `01a07bc9-31a9-7f10-81ec-eee5b5302a80`。
元の依頼には、実データでの確認と、無関係な判定変更による誤通知を避ける目的が明記されていた。
追加する判断基準は、目的から合格条件を作り、省略・移行の各経路で、保存した判定が利用先まで届くことを確認するためのものとする。
「もっと確認するようユーザーが指示すべきだった」という解釈は採らない。

以下の参照位置はJSONLの行番号で示す。専用コマンド `hiho_get_codex_session_jsonl_paths` で対象とフォーク元を取得した。

- [対象ログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T21-12-58-01a07bc9-31a9-7f10-81ec-eee5b5302a80.jsonl)：マージ後の実行、移行・再利用の不具合修正と確認。
- [移行方針のログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T16-57-14-01a07adf-0fbd-7cc1-a490-f485dfedfead.jsonl)：互換性禁止の解釈を訂正し、自動移行と切替手順を採用。
- [CI失敗への提案ログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T16-35-42-01a07acb-5a8b-7242-ac94-bf96254cc201.jsonl)：旧保存形式を読めない問題と、後に見直された切替案。
- [元の依頼ログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T11-21-17-01a079ab-8085-7771-ac10-a142736898fa.jsonl)：要素別のAI推論を設計・実装した依頼。

## 目的と確認の必要性は、実装前に伝えられていた

元の依頼ログ343行では、無関係な変更で待ち相手が変わると通知の信頼を失うため、更新要素を限定したいと述べている。568行ではAI呼び出し数の削減も目的として明示し、609行では期限とアサインの二例だけに対象を限定しないよう指摘した。
813行では、途中のチェックポイント、実データ構造との照合、モックに限らない実行、複数例の確認を求めている。手元での確認用コードは許可し、コミットは禁止していた。設計変更が必要なら設計スキルへ戻り、その場しのぎの迂回を避けるよう指示している。

4099・4116行のCodex報告には、実AI3項目と、固定AI出力による通常処理・保存・再読み込みの確認がある。一方、全8要素の実AI出力や、失敗・延期・依存関係を組み合わせた連続運用全体は未確認だと明記している。
このため、「一切確認しなかった」とは評価しない。確認した事実から目的の達成を判断するには、対象となる分岐と観測箇所が不足していた、という学びにする。

移行方針のログ528行では、採用値、保存失敗後の再試行、通知候補、初回実行まで含む提案を示し、535行でユーザーが実施を依頼した。既存の目的をさらに具体的な実装確認へ落とすことが、今回補う手順である。

## 移行・再利用のどこで役目を失ったか

### 移行対象を最終状態の分類で狭めていた

対象ログ903・912行のコード出力では、`createLegacyAdoptedElements` が `decisionBasis === "ai_only"` を状態・待ち相手・次の行動の移行条件にしていた。
957行のCodex調査では、AIが抽出した情報を規則に通して得た判定を対象外にしたことが、実質的な担当者や返信待ちの取りこぼしにつながったと説明している。1100行では、採用済み関係の引継ぎ漏れも報告した。
1569行では、「採用済みの判定を保持する」という既存方針に実装が違反していたと整理している。

修正途中にも、新規AI出力の上限に合わせて過去の根拠を切り捨てる問題が1560行で見つかり、1811行では、旧処理にない根拠分類を通知の移行条件にした問題が見つかった。
2126行には、問題が出た4件で延期後も待ち相手・次の行動を保持し、移行結果を渡さない対照では不具合が再現したとの報告がある。2297行では、新規出力の上限超過を拒否しつつ、長い移行根拠を保存・再読込でき、通知の信頼度も上がらないことを確認している。
これらは [PR #117](https://github.com/VOICEVOX/voicevox_task_tracker/pull/117) の修正と確認に対応する。

採用する規則は、旧処理での利用と採用条件から対象を決め、保存値と新規出力の入力境界を分けること。未採用候補まで復元する規則にはしない。1709行では、採用を証明できない進捗判定を復活させず、保存済みの進捗日時を保つ判断も報告されている。

### 新規生成の入力条件が、再利用だけの項目を止めた

対象ログ3574行では詳細情報を根拠一覧へ加える20行の修正案を報告したが、3757行で、確認入力のID付与が本番と一致しないため取り下げている。
4453行の実行出力では、公開境界などで除外した2件を除く143件の詳細情報を通常処理へ通し、1件で過去の根拠参照が現在の入力にない例外を再現した。4497行の出力では、その項目の全8要素が `up_to_date` で、再推論の選択は空だった。
4589行の報告どおり、AIを呼ばない項目も呼び出し前の入力準備で停止していた。

4663行には、同じ143件で修正後の入力検証失敗が0件となり、該当項目の8要素をAI呼び出しなしで保持できたとの報告がある。4823行では、判定値・根拠・生成記録・待ち時間の起点を保持して後続処理を通過したと報告している。
[PR #118](https://github.com/VOICEVOX/voicevox_task_tracker/pull/118) では、AI入力用の固定値と、保存する完全な判定結果を分けた。現在の出力が参照する根拠の検証は維持している。

ここから、再利用時にも必要な検証と、新規生成にだけ必要な検証を区別する。過去の根拠を保存から消したり、現在の根拠参照の検証を一律に緩めたりする判断は採らない。

### 生成を省く分岐で、下流への適用も抜けた

対象ログ5628行では、AIを呼ばない項目で保存済みの関係判定をグラフへ渡さず、関係が失効する経路を確認した。
5849行で一方の省略経路を修正した後、5970行では解析自体を省く経路にも同じ受け渡しが必要だと報告した。監査を前回の復元対象だけから修復前に有効だった786関係へ広げると、同じ問題で失効した関係が75件あり、うち8件が依存関係だった。
6390行では両省略経路から関係と通知推奨が下流へ届くことを確認し、[PR #119](https://github.com/VOICEVOX/voicevox_task_tracker/pull/119) へ反映した。

6747行に収録された担当エージェントの実行報告では、ある項目の全8要素が再利用可能でも、グラフなしの一段目だけでは待ち状態が一致しなかった。通常の `reduceAllAnalyses` の二段階処理と既存グラフを通すと、待ち相手・次の行動・時間の起点・採用値が一致した。
6987・7139行では、修復処理が本番の信頼度計算を省いたため、76関係中39件で信頼度が異なることも報告している。この76件は追加調査後の修復対象であり、先の75件とは集計範囲が異なる。
関係の存在や保存値の一致だけでなく、通常の適用処理による派生値まで確認する根拠になる。

## 正当な変更を、欠落と混同しない

対象ログ5308行では、延期された31項目の採用値を欠落と報告したが、5412・5628行で訂正した。項目全体は延期でも、終了したPRの待ち相手などは確定情報によって不要になっていた。項目単位の実行状態だけで、各要素の保持・取消を判断してはいけない。

7616行では、通知を保留した実行後に、入力と採用値が同じなのに判定が変わった項目0件、採用済み関係の理由のない失効0件を報告した。7620行の監査出力でも、同一入力の派生差分が0件と確認できる。有効関係の減少は、新規採用による取消61件と端点の除外1件に分類されていた。
この結果を「関係が一切減らないことが正しい」という基準に置き換えない。
通常実行後の8193・8297行では、関連候補の変化で入力が変わった1項目は再推論対象となり、延期中に4関係が一時的に無効になる制約を明示している。すべての入力変更時にも保持が保証されたとは扱わない。

## 改善後に期待する判断と避ける判断

以下は元の依頼、ログの修正結果、関連コードから作った確認材料であり、実行済みのエージェント評価結果ではない。

| 条件 | 期待する判断 | 避ける判断 |
| --- | --- | --- |
| 期限だけ更新し、待ち相手の変化による通知を避けたい | 呼び出し数、対象外の採用値、下流の待ち相手と通知候補にそれぞれ合格条件を置く | 期限の更新成功と全体の終了コードだけで完了する |
| 利用者は期限とアサインを例示した | 既存の全要素と分岐から、目的に関係する確認対象を洗い出す | 例示の二条件だけを対象にする |
| 旧AIの解釈を規則へ渡して担当者を決めていた | 旧採用条件と保存値を照合し、延期時にも同じ判断材料を下流へ渡す | 最終分類が決定論的なので移行対象外にする |
| 新規出力の上限より多い根拠が保存時に集約されている | 保存値の契約を分け、根拠を欠落させず、新規出力の上限は維持する | 保存のために根拠を切る、AI出力も無制限にする |
| 全要素を再利用できるが、過去の根拠は現在の取得範囲にない | 通常の入力準備を通して呼び出し不要で進むことと、保存した根拠の不変を確認する | 再利用のために過去の根拠を取り直す、新規出力の根拠検証を消す |
| 一部要素は新規生成し、残りは固定値として使う | 固定値への投影と完全な保存結果を分け、新規出力は現在の根拠で検証する | 投影した値で保存結果まで置き換える |
| AIだけを省く経路と、解析対象から外れる経路がある | 両方の呼び出し元から、採用値が通常の適用処理とグラフ・通知へ届くことを確認する | オブジェクトの保持や一方の分岐だけで両方を保証する |
| 項目全体は延期だが、終了済みのため待ち相手は不要 | 要素ごとの必要性を確認し、確定情報に基づく取消を維持する | 延期だから全採用値を復元する |
| 最新の採用結果が関係なしとなった | 採用を確認した取消として扱う | 空の結果を保存欠落とみなし、旧関係を復活させる |
| 手作りの確認入力で根拠の参照エラーが出た | 本番の正規化・ID付与・対象選別を通した入力で再現性を確かめる | 本番で生成されない中間入力に合わせてコードを修正する |
| 既知の関係を復元し、保存と再読込に成功した | 変更前の利用対象から照合範囲を作り、通常の信頼度計算・二段階の判定・通知候補まで確認する | 復元した関係の存在だけを数える、派生値を独自計算で補う |
| 本文と採用値は不変だが、関連候補が変わった | 要素が依存する入力全体と、許された取消条件を調べる | 本文不変だけを根拠に不具合と断定する |

2026-09-08の文面点検では、現行コードの `src/persistence/snapshot-migration.ts` にある `createLegacyAdoptedElements`・`adoptedRelationCandidates`、`src/codex/input.ts` の `projectCodexLockedElementResult` を照合した。
省略経路は `src/cli/production-runtime.ts` の `reductionForAnalysis`・`codexOutputForConsumers`・`reduceAnalysisPass` と、`src/codex/reducer.ts` の `reducePreservedCodexRelationsAndNotification` を参照した。開発手順の `docs/DEVELOPMENT.md` には、固定値と過去根拠の分離、両省略経路の検証が記載されている。
現在のコードを過去の不具合の証拠にはせず、当時の根拠には上記ログを用いた。Gitによる版の特定は行っていない。次の適用時には、現在の関数配置と呼び出し経路を読み直す。

## 採用範囲と確認の限界

設計用の `refined-codedesign-hiho` には、再利用・移行・切替の判断基準がすでにある。実装用には、分類条件、入力準備、returnやcontinue、下流へ渡す引数や配列、通常の計算処理という具体的な確認箇所を補った。
「互換性禁止」を移行の削除まで広げた問題は、移行方針のログ7・243行で、ユーザーの禁止ではなくCodexの設計判断だったと訂正している。この適用範囲の規則は設計用スキルにすでにあるため、重複追加しない。
今回の手動修復、workflow停止、通知保留を全プロジェクトの常設手順にはしない。対象ログ3108・3145・3179行のやり取りでも、保存形式変更時の移行と、障害固有の手動修復を分けている。

既存の[型・入力条件・ビルド経路の確認材料](session-01a07896-34af-7d52-bf96-6059e7ac962b.md)と[設定順序・コメントの確認材料](session-01a07ce4-7270-7b32-8820-ecfa7b206d96.md)も文面で照合した。入力境界の検証を保つ規則や、設定の呼び出し関係と実際の整列結果を確認する規則は維持している。
今回の確認は、依頼・指摘・修正結果・現行コードの照合、確認材料との文面点検、文章lint、公式 `skill-creator` の `quick_validate.py` による形式検証である。
プロダクトのテストは実装せず、実データでの再実行、実AIによる再実装、独立エージェント評価は行っていない。過去ログの成功を今回の実行結果として扱わず、実装品質の改善を実測したとは報告しない。
