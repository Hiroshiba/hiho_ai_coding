# 型・入力条件・ビルド経路の判断を確かめる

対象はセッション `01a07896-34af-7d52-bf96-6059e7ac962b`。
以下の「対象ログ」は[元ログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T06-18-24-01a07896-34af-7d52-bf96-6059e7ac962b.jsonl)を指し、参照位置はJSONLの行番号で示す。
実装対象は [VOICEVOX/voicevox PR #3100](https://github.com/VOICEVOX/voicevox/pull/3100)。macOSのコード署名対応から、エンジン配置のafterPackへの共通化と細部の整理へ続いた作業である。
フォーク元の[最初の依頼ログ](/home/hihok/.codex/sessions/2026/08/31/rollout-2026-08-31T05-30-33-01a0545d-e157-7ac3-922e-8b45fb39200b.jsonl)の107行では、Engine・Core・ONNX Runtime Builderの署名処理との整合性と保守性を重視する意図が示されている。
フォーク元は依頼と判断の前提を補うために参照し、そこにある別件の全指摘を今回の追加対象にはしていない。

## 指摘と修正結果

### 型・配置・命名

- 対象ログ52行で、ユーザーは `build/types.ts` の型を `build/afterPack.ts` へ移せるか確認し、可能なら移すよう依頼した。97行の差分では定義を移し、呼び出し側のimportを変更して、型だけのファイルを削除している。複数の利用元があっても、一つの処理の引数契約を独立ファイルへ置く必要があるとは限らないという判断へ一般化する。
- 168行では、ユーザーが `{ mode: "none" } | { mode: "copy" | "move"; directory: string }` を提示し、`directory` を二重定義しない形を希望した。221行の差分と575行の報告で、識別値をまとめた型と、それを使う処理への反映が確認できる。異なる付随データを持つ分岐までまとめる指示ではない。
- 314・333行では、省略可能な `void` は周囲が省略していれば省き、周囲に合わせるよう明示した。904行では補助関数とdefault exportの順序を他のコードから決めるよう求め、1176行の報告では `playwright.config.ts` を根拠に配置している。
- 1179行では、不要な空行と、短い呼び出し・型・enumの複数行化を指摘した。1366行の報告では、一行にできる箇所だけを変更し、Prettierが複数行へ戻す81文字のenumは維持した。短さのためにフォーマッターを変更する要求ではない。
- 3553・3569行では、曖昧な「配置設定を解決する」を見直し、ユーザーが `parseVoicevoxEnginePlacementFromEnv` と「環境変数からVOICEVOX ENGINEの配置設定を得る」を選んだ。句点は既存コメントに合わせるよう指示している。Codexが3562行で最初に推した別案を、ユーザーの選択として扱わない。
- 3609〜3699行で、ユーザーは `hasDirectoryValue`、`VoicevoxEnginePlacement`、`placeVoicevoxEngine`、`getMacosResourcesPath`、`getMacosContentsPath` を選んだ。3771行の差分で型・関数・変数・docstringへの反映を確認できる。3842・3861行では外部設定名も `VOICEVOX_ENGINE_PLACEMENT_MODE` へそろえるよう依頼した。
- 3901行でCodexは `source`・`destination` は実際のコピー元・コピー先なので維持したと説明している。これは意味ごとに改名対象を区切った実装側の判断であり、ユーザーの明示的な肯定ではない。4052行でユーザーが求めたdocstring「VOICEVOX ENGINEを配置する」への短縮は、4107行の差分で確認できる。

### 入力の検証と簡略化

- 対象ログ440・582行では、環境変数の `none` を受け付け、非空ディレクトリとの組合せをエラーにする変更をユーザーが提案・採用した。この時点では `copy`・`move` と未指定ディレクトリから `none` へ変換する挙動が残っていた。
- 2009・2036行では、二つの環境変数からZodで最終的な直和型を得て、変換関数をなくす案を求めた。2109行のCodex案は既存分岐を `transform` へ移したものだった。2116行でユーザーは「transformに関数移しただけ」と指摘し、独自エラーメッセージにはこだわらないと明示した。
- 2312・2473行では、条件が分かりやすく短い形を引き続き求め、Zodを使う案では型をschemaから作ることを希望した。2671行でユーザーがobjectのunionを提示すると、2724行のCodex回答は、`copy`・`move` と空ディレクトリを拒否する仕様にすれば簡潔になると説明した。既存の自動変換を守る前提が複雑さの原因だったという説明はCodexによる分析である。
- 2731〜3037行では、`default("none")` の必要性を検討した。Codexは2755行でこの記法を必須と評価したが、2882・3030行で、必要なのは未指定から `none` を作る処理であり、記法は代替できると訂正した。使用版はZod 4.4.3。検証、分岐選択、値の補完を混同しない規則へ採用する。
- 3037行のユーザー例は `mode: "none"` を明示していたため成功する。3062行で失敗条件を求められ、3080行でCodexが入力の組合せ表を示した。3087行でユーザーは「その条件を見落としていました。両方未指定だとたしかに none になってほしい」と述べ、default付き案を採用した。3299行で実装と検証の完了が報告されている。
- 3336行では、ユーザーがZodを使わず、型をafterPack側、環境変数の変換を設定側へ置く案を依頼した。3390行の短い案に対し、3397行ではエラーメッセージではなく「論理構造がぱっとわからない」と指摘した。3446行でCodexが既定値の補完を先に済ませ、二つの正常条件を同形のifで並べる案を提示し、3453行で採用された。3494行の差分と3543行の報告で反映を確認できる。
- 3944・4029行では、ユーザーがモードの既定値を常に `none` とする変更を提案・採用した。4107行の差分で、ディレクトリの有無による既定値の切替がなくなった。最終的には両方未指定なら `none`、ディレクトリだけ指定するとエラーになる。途中の「DIRだけならcopy」を将来も守る仕様として残さない。

重複検証については、フォーク元の[入力検証を整理したログ](/home/hihok/.codex/sessions/2026/09/07/rollout-2026-09-07T04-42-07-01a0783e-0bb3-7050-814c-66e5fb74f09d.jsonl)の12・182行で、ユーザーが既に検証した値に意味のないschemaを作らないよう求めている。
175行のCodex調査では、検証済みの値から列挙したプロパティだけで作るオブジェクトを、保存・再読込せず直接afterPackへ渡していた。472行で重複schemaとparseの削除を報告した。外部入力の初回検証は維持している。
その後のZod利用・不利用の選択は個別の採用判断であり、「Zodを常に使う」「Zodを避ける」のどちらにも一般化しない。

### 補助処理・環境変数・配置先

- 対象ログ7行ではLinux本体のchmod、1373・1398行ではmacOS Helperのchmodとローカライズ用ディレクトリの作成が今も必要か、文書とコードから判定するよう求めた。
- 208行にはBuilderの展開時のUnixモード復元と、その後の改名処理がある。1700行のツール出力では、Electron 42.1.0の公式ZIPのx64・arm64双方でHelper 4種が `100755`、`ja.lproj`・`en.lproj` が `40755` だった。1546行にはBuilderの言語選別処理、1736・1770行には補助処理を削除した差分がある。
- 2002行のCodex報告では、採用版Builder 26.15.2が権限と対象言語を保持し、元の復旧理由だったArtifacts受け渡し工程は [PR #1243](https://github.com/VOICEVOX/voicevox/pull/1243) でなくなったとして削除している。配布用ビルドは再実行していないとも明示した。同梱エンジンの `run` へのchmodは残っており、全chmodを不要とした結論ではない。
- 582・892行では、Linuxの実ファイル名とchmod対象名の不一致、および影響がない条件の根拠をユーザーが求めた。673行の実装では、実行ファイル名の既定値は `sanitizedName.toLowerCase()`、`productFilename` の既定値は `sanitizedProductName` だった。885行のCodex説明では、CIは実行ファイル名を明示するので一致するが、未指定時には `voicevox` と `VOICEVOX` が異なる。chmod削除と、名前の修正は区別する。
- 4001行でCodexは、モードの既定値をnoneにすると、後続のprepackagedビルドでDIRを空文字にする必要があると説明した。4008行の指摘を受け、4022行ではjob共通のDIRが残っている前提だったと補った。4029行でユーザーは共通定義の削除と、必要な `format()` の使用を採用した。4107行の差分では、取得先と最初のビルドだけにパスを指定している。後続stepへの空文字追加を不可避としない判断へ採用する。
- 4119・4217行では、macOSの配置先の変更と、その導入・取り残された経緯を求めた。4210行のCodex比較では、CIは変更前後とも `Contents/Resources/vv-engine`、旧ローカルextraFiles経路だけが `Contents/MacOS/vv-engine` から変わる。標準の起動設定は `../Resources/vv-engine/run`、基準は実行ファイルのある `Contents/MacOS` だった。
- 4363行には [PR #2112の作者の回答](https://github.com/VOICEVOX/voicevox/pull/2112#issuecomment-2145087652)「はい移さないとコード署名が失敗します」と当時の差分がある。4389行の最終説明は、CI側の配置先と起動設定だけが更新された事実と、extraFilesを個別に残した意図は不明であることを分けている。この区別を採用し、すべてのローカル実行が壊れていたという解釈は採らない。

次の実装で参照するコードは、VOICEVOX/voicevoxの `build/afterPack.ts`、`build/electronBuilderConfig.ts`、`.github/workflows/build.yml`、`build/.env.embed-engine.macos`、`src/backend/electron/main.ts` の `getAppPaths`。
Builder側は採用版の `out/util/electronGet.js` の展開処理、`out/electron/ElectronFramework.js` の改名・言語選別、`out/linuxPackager.js` と `out/appInfo.js` の名前生成を参照する。
2026-09-08の文面点検時には、ローカルの `hiho_git_worktrees/engine_packaging_afterpack` でも、最終の型・入力検証・環境変数の範囲・配置先と起動設定の対応を確認した。Gitによる版の特定は行っておらず、履歴上の根拠は上記ログの差分を用いる。次の適用時には採用版と現在の経路を読み直す。

## 改善後に期待する判断と避ける判断

以下は元の指摘を基にした確認材料であり、実行済みのエージェント評価結果ではない。

| 条件 | 期待する判断 | 避ける判断 |
| --- | --- | --- |
| 呼び出し側と処理側だけが一つの引数型を使う | 処理側のモジュールへの配置と、依存・循環参照への影響を確認する | 複数ファイルから使うという理由だけでtypesファイルを維持する |
| copyとmoveは同じ必須データを持ち、noneは持たない | copyとmoveを同じ型分岐にまとめ、利用側の絞り込みを確認する | 同じデータを二重定義する、異なるデータが必要な分岐も一律にまとめる |
| 新規ファイルだけ `void` と句点が付き、周囲は省略する。短い型は一行にできる | 同種の既存コードとフォーマッターに合わせる | 新規ファイル内の自己一致を根拠にする、整形設定を変えて一行を強制する |
| 配置元と配置方法を表す型を改名する | 型から外部設定まで同じ概念の名前を照合し、実在確認と入力値の有無も区別する | 環境変数名を取り残す、意味の異なるsource・destinationまで改名する |
| 検証済みの値を型付きオブジェクトにして直接渡す | 初回検証を保ち、再検証が新たに防ぐ入力を確認する | 型定義のためだけにschemaとparseを足す、外部から読み直した値の検証まで消す |
| 簡略化案が既存関数をtransformへ移しただけ | 受理する組合せと値の変換を分け、仕様変更を伴う簡略化は条件の違いを示す | 関数が消えたことだけで簡潔になったと報告する、旧フォールバックを黙って固定する |
| 明示的なnoneでは成功するが、両方未指定だと失敗する | 失敗する入力を示し、補完の必要性とdefaultというAPIの選択を分ける | 必須APIだと言い切る、ユーザーの成功例を失敗する例として扱う |
| 短いifの次の正常条件が、先の否定条件に依存する | 補完を先に行い、正常な組合せを並列に読める構造にする | エラーメッセージだけ詳しくする、行数だけで比較する |
| 権限と空ディレクトリを復旧する古い処理がある | 元の欠落原因、配布アーカイブ、採用版の展開と後続の受け渡しを照合する | 最新ツールなら不要と推測する、別経路のエンジンのchmodも削除する |
| CIは実行ファイル名を明示するが、ローカルでは省略する | 生成する名前と参照する名前を、明示・未指定の両方で比較する | CIが通るのでproductFilenameで実ファイル名を得られると判断する |
| モードの既定値をnoneにし、job共通のDIRが後続ビルドへ継承される | 各ビルドの設定読込を確認し、必要なstepだけへ渡す案も比較する | 後続stepの空文字上書きだけを必須とする、DIRだけでcopyになる中間仕様を残す |
| CIとローカルで配置経路が異なり、片方だけ配置先が変わる | 経路別の配置先と起動パスの基準を対応付け、影響条件を説明する | 全経路で不変と報告する、外部エンジンを使う開発実行まで壊れていると広げる |

既存の[設定順序とコメントの確認材料](session-01a07ce4-7270-7b32-8820-ecfa7b206d96.md)も引き続き使う。
型・関数の短縮は、制約を見直すために必要な理由コメントを削除する指示ではない。ビルド補助処理の削除も、同じ入口を持つプラグインの選択・呼び出しを追う既存規則を置き換えない。

## 採用範囲と確認の限界

型・関数・命名の照合、入力条件を直接読める構造、補完と特定APIの区別、成果物と設定の経路を追う判断を追加する。
途中のZod案、DIRだけでcopyにする既定値、formatを避ける制約は、最終判断で変更されているため固定しない。最終の「既定値はnone」もこの配置設定の仕様であり、他の設定に一般化しない。
`refined-coding-hiho` の継続する判断基準の文書化や、`refined-codedesign-hiho` の版・根拠の照合は重複追加せず、今回の具体的な呼び出し・入力・成果物の確認へ適用範囲を絞った。
エージェント分担、Git操作、pushやCI待機中の連絡は、今回の実装用スキルに追加しない。

今回の検証は、指摘・採用案・ログの差分・関連する現行コードの照合、上表と既存確認材料の文面点検、公式 `skill-creator` の `quick_validate.py` による形式確認とする。
実際のCodexによる再実装、独立エージェント評価、入力検証の再実行、配布用ビルドや起動確認は行っていない。実装品質の改善を実測したとは扱わない。
