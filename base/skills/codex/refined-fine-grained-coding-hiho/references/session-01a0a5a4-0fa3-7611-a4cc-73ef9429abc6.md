# 入力検証テストの境界と既存構造を確かめる

対象はセッション `01a0a5a4-0fa3-7611-a4cc-73ef9429abc6`。
[指定ログ](/home/hihok/.codex/sessions/2026/09/16/rollout-2026-09-16T00-16-27-01a0a5a4-0fa3-7611-a4cc-73ef9429abc6.jsonl)自体はフォーク時のメタデータだけを持つ。
以下の参照位置は、フォーク元に含まれる[テスト方針の検討ログ](/home/hihok/.codex/sessions/2026/09/12/rollout-2026-09-12T19-31-39-01a0952c-3e18-7d61-a2c8-cedcae6d1e8f.jsonl)、[実装ログ](/home/hihok/.codex/sessions/2026/09/12/rollout-2026-09-12T19-53-30-01a09540-3d4a-7712-a242-d8544954d62f.jsonl)、[後続ログ](/home/hihok/.codex/sessions/2026/09/15/rollout-2026-09-15T23-56-45-01a0a592-073c-7ef2-8ea4-319fbb5e8e1c.jsonl)のJSONL行番号で示す。
対象変更は [VOICEVOX/voicevox_engine PR #1883](https://github.com/VOICEVOX/voicevox_engine/pull/1883) に追加された入力値域のE2Eテストを整理する作業である。

## 指摘と修正結果

### 宣言的制約と独自判定を分ける

- テスト方針の検討ログ21行で、利用者は「モデル境界で各値域を確認する」というCodexの提案が方針のどこから導かれたかと、既存例の有無を確認した。97行でCodexは、方針にモデル直接テストの指定はなく、モデル境界を必須とした判断を撤回した。
- 実装ログ795行で取得した [Issue #1886](https://github.com/VOICEVOX/voicevox_engine/issues/1886) は、PydanticのFieldやFastAPIのQueryへの宣言だけで完結する入力制約を個別にテストせず、独自判定ロジックは引き続きテストする方針を示している。この方針はVOICEVOX固有であり、他のプロジェクトへ無条件に広げない。
- 実装ログ811行で、利用者はPRの新規テストを削除する変更を依頼した。888行でCodexは、Fieldによる制約と `AccentPhrase.validate_accent` の独自判定を分け、後者だけを残すと判断した。1022行では独自判定の負例を合成APIのE2Eテストへ移したと報告した。
- 実装ログ1031行で、利用者はこの負例をAPIテストではなくモデルのテストへ置くべきだと指摘した。当時の [`AccentPhrase.validate_accent`](/home/hihok/Github/voicevox_engine/hiho_git_worktrees/pr1883-followup-9e8bb91d/voicevox_engine/tts_pipeline/model.py:53) は、モデルのモーラ数とアクセント位置を比較する独自validatorだった。HTTP固有の処理を通さなくても、モデルの直接構築で判定を観測できる。

### 類似テストとの一致を具体化する

- 実装ログ1070行で、Codexは `test/unit/tts_pipeline/test_model.py` を新設し、最小の入力をその場で作る一つのテストを提案した。1079行で利用者は、他のモデルテストも探すよう求めた。
- 1111行でCodexは、Pydanticモデルの独自validatorを直接確認する実質的に唯一の類似例として [`test_user_dict_model.py`](/home/hihok/Github/voicevox_engine/hiho_git_worktrees/pr1883-followup-9e8bb91d/test/unit/user_dict/test_user_dict_model.py:1) を挙げた。それでもファイル名とテスト構造を合わせず、最初の提案を維持した。
- 1120行で利用者は、類似例との差があること自体を問題とし、きめ細やかな実装スキルを再実行して再提案するよう求めた。既存例は `_UserDictWordInputs`、`generate_model`、有効な基準入力のテストを持ち、アクセント上限の負例では基準入力の `accent_type` だけを変えて `ValidationError` を確認している。
- 1147行でCodexは、配置だけでなく、ファイル名、補助型、基準入力生成、正常系、変更する一項目、例外型、コメント区分、テストクラスの有無まで対応付けた。`test_tts_pipeline_model.py` という名前や補助構造はCodexの再提案であり、利用者が明示的に採用した結果ではない。将来の固定形式にはせず、類似実装との差を列挙して必要な差だけを残す確認材料として扱う。

### 必要性と現在の変更範囲を分ける

- 後続ログ8行で、利用者は独自validatorのテスト自体は書くべきだと認めつつ、PRからは一旦なくし、PRコメントへ残すと決めた。38行でE2Eに残っていたテストの削除、159行で新規テストが0件になった最終結果が報告された。
- したがって、モデル単体テストが妥当という判断は、現在のPRへ含める根拠にはならない。実装に含める範囲は利用者の明示した延期を優先する。

## 改善後に期待する判断と避ける判断

以下は元の指摘を基にした確認材料であり、実行済みのエージェント評価結果ではない。

| 条件 | 期待する判断 | 避ける判断 |
| --- | --- | --- |
| プロジェクトがライブラリへの宣言だけで完結する制約を個別テストしない方針を定めている | FieldやQueryの負例をAPIとモデルの両方から外し、独自判定だけを分ける | 削除したE2Eの全ケースをモデル単体テストへ移し替える |
| モデルの複数項目を使う独自validatorの負例を確認する | API固有の契約が別にないか確認し、なければモデルを直接構築する単体テストへ置く | HTTP 422で観測できるという理由だけでAPI E2Eへ置く |
| モデル直接テストを探すと、同じ例外と独自validatorを確認する類似例が一つ見つかる | 配置、ファイル名、入力生成、正常系、変更箇所、例外確認、コメントとクラスの有無を対応付け、必要な差だけを説明する | 実装ファイル名だけから `test_model.py` と決める、同じディレクトリなら十分に一致するとみなす |
| 方針文にはAPIで細かな入力を繰り返さないとだけ書かれている | 方針が指定する境界とCodexの推論を分け、責務と既存例から候補を比較する | 方針にないモデル直接テストを必須と断定する |
| テストの価値は認められたが、現在のPRから外して後で扱うと決まった | 現在の差分から削除し、指定されたPRコメントや後続課題へ分ける | 技術的に必要という理由で現在の差分へ残す |

## 採用範囲と確認の限界

採用する学びは、入力検証を担う責務からテスト境界を決め、最も近い既存テストの配置だけでなく構造上の差まで照合し、テストの必要性と現在の変更範囲を分けることである。
VOICEVOXで宣言的な制約を個別テストしない方針、提案されたファイル名や補助型は、他のプロジェクトやすべてのモデルテストへ固定しない。
Issue文面の構成やレビューの自信度、Git操作とsubagent運用は、今回の実装用スキルへ統合しない。

2026-09-16に、対象ログとフォーク元の発言、PR作業用worktreeのモデル、既存モデルテスト、途中のE2E差分、最終報告を照合した。
プロダクトコードの変更、新しいモデル単体テストの実装、独立エージェントによる再実装評価は行っていない。変更後のスキルが実際の実装品質を改善したかは未検証である。
