# 真偽値環境変数と特殊値への変換を確かめる

対象はセッション `01a0a559-2470-7733-93c0-3efbb71fe9b2`。
以下の「対象ログ」は[元ログ](/home/hihok/.codex/sessions/2026/09/15/rollout-2026-09-15T22-54-37-01a0a559-2470-7733-93c0-3efbb71fe9b2.jsonl)を指し、参照位置はJSONLの行番号で示す。
フォーク元の[調査ログ](/home/hihok/.codex/sessions/2026/09/15/rollout-2026-09-15T20-33-34-01a0a4d7-fecc-7ea0-a115-231107225f71.jsonl)では、[VOICEVOX/voicevox PR #3092](https://github.com/VOICEVOX/voicevox/pull/3092)の手動ad hoc署名をElectron Builderへ移せるか調べている。
さらに前のセッションで指摘された環境変数列の順序漏れは、[環境変数列の順序漏れに関する確認材料](session-01a0a462-051f-74f3-bf1d-f9d10d1b2de7.md)で別に扱う。

## 指摘と修正結果

- フォーク元ログ255行で、利用者は `identity: "-"` によって後段の手動ad hoc署名を不要にできるか尋ねた。374行では、アプリ内部も再帰的に署名されるか追加調査を求めた。対象ログ7行では、常にad hoc署名する意図ではないとして、コード内コメントを分かりやすくするよう求めた。
- 対象ログ114行でCodexは `identity: "-"` がad hoc署名を明示的に選ぶ設定だと確認し、無条件に残すと正式署名へ切り替わらないと報告した。121行で利用者はその競合を問題にし、正式署名用PR上へ実装を加えた差分を求めた。
- 414行の最初の差分案は、正式署名しないmacOSのBuildコマンドへ `--config.mac.identity=-` を条件付きで加えた。423行で利用者は、CLI上書きが既存のビルドコードと合わない可能性を指摘し、環境変数による案か再設計を求めた。
- 590行の再設計では `CSC_NAME` をworkflowから渡し、TypeScript設定で `identity` へ橋渡しする案を提示した。しかし採用版Electron Builder 26.15.2では、`CSC_NAME` は証明書名の検索条件であり、環境変数として `"-"` を渡すだけではad hoc署名を選ばないことも確認している。
- 607行で利用者は、ad hoc署名するかを表す専用環境変数を作り、workflowの署名条件から渡して、設定側で `"-"` へ変換する案を選んだ。名称、定義位置、順序を周囲へ特に合わせるよう求めた。
- 791行の差分は `MACOS_AD_HOC_CODE_SIGNING` を `z.enum(["true", "false"]).optional()` で受け、利用側で `=== "true"` と比較していた。798行で利用者は、未指定を `false` にし、可能なら文字列をbooleanへ変換し、コメントを `// NOTE: "-"を指定するとelectron-builderがad hoc署名する` にするよう明示した。
- 856行の担当エージェント報告は、`z.enum(["true", "false"]).default("false").transform((value) => value === "true")` なら受理する文字列を増やさず、未指定をfalse、parse結果をbooleanにできると確認した。`z.coerce.boolean()` は文字列 `"false"` をtrueにするため不採用とし、`z.stringbool()` の既定語彙は要件より広いと報告した。
- 913行の検証報告では、未指定、`"true"`、`"false"` がそれぞれbooleanのfalse、true、falseになり、空文字と不正値は `ZodError` になること、identityはtrueのときだけ `"-"` になることを確認した。987行で、指定された変更をコミットしてPRへpushしたと報告している。

指摘後のコードは、`hiho_git_worktrees/macos_code_signing` の `build/electronBuilderConfig.ts` 35〜39行で環境変数をbooleanへ変換し、182〜183行でコメントと `identity` への対応付けを行う。
`.github/workflows/build.yml` 327〜339行では、Build Electron stepだけがmacOSと正式署名の条件から `true` または `false` を渡す。
採用版の `app-builder-lib/out/mac/MacTargetHelper.js` 37〜43行は設定のqualifierが `"-"` の場合にad hoc用Identityを作る一方、`out/codeSign/macCodeSign.js` 261〜274行は `CSC_NAME` を証明書の検索条件として使う。
次に適用するときは、対象プロジェクトが採用する版と現在の設定経路を読み直す。

2026-09-17のスキル改善時にZod 4.4.3で同じschemaを再実行し、未指定、`"true"`、`"false"`、空文字、不正値の結果と、`z.coerce.boolean().parse("false")` がtrueになることを確認した。

## 改善後に期待する判断と避ける判断

以下は元の依頼と指摘を基にした確認材料であり、実行済みの独立エージェント評価結果ではない。

| 条件 | 期待する判断 | 避ける判断 |
| --- | --- | --- |
| 真偽値の環境変数で、未指定は無効、受理する文字列は `"true"` と `"false"` だけ | 入力境界で文字列を検証し、未指定をfalseへ補完してbooleanへ変換する。未指定、両文字列、空文字、不正値を確認する | `.optional()` のまま文字列とundefinedを下流へ渡す、truthy変換で `"false"` をtrueにする |
| workflowの真偽値から外部ライブラリの特殊値 `"-"` を選ぶ | workflowでは動作の真偽を渡し、設定境界でbooleanから特殊値へ対応付ける。正式署名、非正式署名、ローカル未指定を比較する | 特殊値をworkflowのCLIへ直接埋め込む、意味の異なる標準環境変数へ同じ文字列を流用する |
| `identity: "-"` の意味がコードだけでは読み取れず、利用者がコメントを求める | 代入箇所で、リテラル、Electron Builder、ad hoc署名という結果を結び付ける | 「署名を任せる」など高水準の目的だけを書く、環境変数名を繰り返す |
| 正式署名では証明書探索を保ち、非正式署名だけad hoc署名する | 真偽値がfalseなら `identity` を指定せず、trueのときだけ `"-"` にする | `identity: "-"` を無条件に設定し、正式署名もad hoc署名へ置き換える |

## 採用範囲と確認の限界

採用する学びは、未指定の意味が無効と決まった真偽値環境変数を入力境界でbooleanへ正規化し、プロジェクトの動作指定と外部ライブラリの特殊値を分けることに限る。
すべての真偽値環境変数をfalse既定にする規則、常にZodの `default` と `transform` をこの順で使う規則、`MACOS_AD_HOC_CODE_SIGNING` という名前、`NOTE:` 接頭辞は一般化しない。
CLI上書きはこのリポジトリの既存経路と合わないため採られなかったのであり、CLI設定自体を一律に避ける規則にはしない。
`CSC_NAME` と `identity: "-"` の違いはElectron Builder 26.15.2で確認した事実であり、別の版やライブラリへ広げない。

環境変数の名称と相対順序は、既存の本文規則と前段セッションの確認材料で既に扱っているため重複追加しない。
エージェント分担、Git操作、commit、push、PR運用は実装判断の規則へ含めない。

今回確認した範囲は、対象ログとフォーク元ログ、指摘後のコード、採用版ライブラリの実装、Zod入力境界の再実行、公式 `skill-creator` の形式検証である。
プロダクトコードは変更していない。
macOS実機のad hoc署名、正式証明書による署名、独立エージェントによる再実装評価は行っていないため、実装品質の改善を実測したとは扱わない。
