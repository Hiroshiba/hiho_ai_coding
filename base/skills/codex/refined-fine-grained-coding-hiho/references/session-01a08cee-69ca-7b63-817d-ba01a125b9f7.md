# 方式変更の採否と提案度を確かめる

対象はセッション `01a08cee-69ca-7b63-817d-ba01a125b9f7`。
以下の「対象ログ」は[元ログ](/home/hihok/.codex/sessions/2026/09/11/rollout-2026-09-11T05-07-10-01a08cee-69ca-7b63-817d-ba01a125b9f7.jsonl)を指し、参照位置はJSONLの行番号で示す。
対象はVOICEVOXのシステムテーマ対応で `.storybook/preview.ts` の実装方法を変更し、後に不要な方式変更を戻した箇所である。
フォーク元のセッション `01a08c73-0e62-71e1-ad7d-045a93ab200c` にはテーマ対応の初期調査があるが、今回の指摘と修正は対象ログ内で確認できるため、そこにある別の判断は追加対象にしない。

## 指摘と修正結果

- 対象ログ5140行で、利用者は `preview.ts` の複雑な変更の目的と必須度を問い、調査と設計を求めた。実装方法の置き換えを依頼した発言ではない。
- 5508行でCodexは、Storybookの公開APIとVueの `watch` を使えば既存の属性監視を削除できるとして、案を「証拠があってそうすべき」と評価した。この時点で案の動作は未検証と明記されていた。確認されていたのは案が成立し得ることと局所的な簡素化であり、今回の変更へ含める優位性ではない。
- 5527行で利用者は、既存から増えた不要な処理を戻すことを求めた。Storybookの仕組みに乗れる案には「乗っかってもいいかなぐらい」と許容を示したが、方式変更を必須とも採用済みともしていない。カスタムテーマについては、機能追加ではなく既存の拡張点を維持する範囲だと明示した。
- 5818行でCodexは、公開APIと `watch` による直接接続を「証拠があってそうすべき」として実装へ進めた。6415行では、専用resolver、HTML属性、`MutationObserver`を削除し、初期化を即時実行する `watch` へまとめ、検証に通ったと報告した。
- 6422行で利用者がデフォルトブランチとの差分の大きさを問い直した。6483行でCodexは、42行追加・40行削除の大半がテーマ反映方式の置き換えであり、その置き換えは必須でなく「証拠はあるがどちらでも良い」だったと訂正した。
- 6490行で利用者は不要な変更を戻すよう依頼し、この判断を本スキルから得られるようにしたいと述べた。6527行でCodexは、本スキルに既にあった「残す差ごとに必要性を示し、必須でない変更を分ける」規則の適用不足を認めた。一般的な差分最小化の再掲だけでは、局所的な利点を採用判断へ昇格させる誤りを防げなかった。
- 6605行で利用者は、新しく生じる不利益は調べつつ、回避が必須でないものは最後の提案へ分けるよう求めた。6954行では、元のアドオン、属性監視、`mounted`と`unmounted`を維持し、差分を12行追加・3行削除に縮小しても、型検査、lint、一時テスト、全OSのE2Eが成功したと報告された。選択テーマを初回から同期反映する案は、任意の追加案として実装から分けられた。
- 6973行で利用者は、`immediate`と`flush`の必須度、テーマ名の表記、組み込み一覧と差し替え可能な一覧の参照経路について調査と設計案を求めた。7334行でCodexは、現在の組み込みテーマでは参照元が分かれていても問題にならないことと、改名は挙動を変えない差分を増やすことを示しながら、名前と参照元を揃える案を「証拠があってそうすべき」と評価した。確認できた構造上の混在は変更候補を示すが、今回採用する優位性までは示さない。本スキルの改善依頼で利用者は、この程度を「そうすべき」ではなく、実装から分ける弱い提案として扱う意図を明示した。

この結果から、案に局所的な利点がある証拠と、その案を今回の実装へ含める判断を分ける。
コードが短くなること、公開APIを使えること、変更後の実装がテストに通ることは、その方式が成立する証拠にはなるが、既存方式を置き換える必要性や総合的な優位性を単独では示さない。
既存方式でも目的を満たせる場合は、必要な接続だけを変える案を比較対象に含め、変更量、責務の移動、既存との一貫性、利用者が明示した優先順位から採否を決める。
利用者が案を許容したことと、採用を選んだことも区別する。

## 改善後に期待する判断と避ける判断

以下は元の指摘を基にした確認材料であり、実行済みのエージェント評価結果ではない。

| 条件 | 期待する判断 | 避ける判断 |
| --- | --- | --- |
| 既存方式でも要求を満たし、別方式には行数削減という確認済みの利点がある | 無変更案と必要部分だけ変える案を含めて得失を比較し、総合的な優位性が確認できなければ任意提案へ分ける | 行数が減る証拠だけで採用を推奨し、実装へ含める |
| 子の描画前に初期化しないと未設定値の参照で例外になる | 初期化という必要な結果と、そのための特定APIや方式を分け、既存方式に足す最小の変更も比較する | 初期化が必須なので、`watch`への全面的な置き換えも必須または推奨だと扱う |
| 利用者が方式変更について「使えるならやってもよい」程度の許容を示し、調査と設計を求めている | 許容と採用指示を区別し、比較結果を提案して選択を待つ | 許容を採用指示に読み替えて実装する |
| 方式変更後のコードが短く、すべてのテストに通る | その方式が成立する確認と、今回採用する価値の確認を分ける | テスト成功を、方式変更自体が必要だった証拠にする |
| 必要な接続だけを変える案でも同じ合格条件を満たし、既存構造を保てる | 利用者が既存との一致や差分最小を優先していれば、その案を採用する | 内部構造として好ましく見える別方式を、利用者の優先順位より上に置く |
| 二つの名前や参照元が混在するが、現在の入力では不具合がなく、整理すると挙動を変えない差分が増える | 混在の事実と実害を分け、読みやすさだけが利点なら根拠の種類を区別して任意提案にする | 混在している証拠を、その整理を採用すべき証拠に読み替える |
| 必須ではない不利益や追加対策が見つかった | 今回の実装から分け、根拠、変更量、残る制約とともに最後に任意提案する | 対策に利点があることだけを理由に同時実装する |

## 採用範囲と確認の限界

採用する学びは、既存方式を置き換える案の評価、提案度、実装範囲の決定に限る。
Vueの `watch`、Storybookの公開API、属性監視のどれかを常に選ぶ規則にはしない。差分が小さい案を常に選ぶ規則にもせず、要求を満たす保証と回避必須の不具合を先に評価する。
対象ログで確認できたのは、利用者の発言、Codexの判断、実装後の報告、不要な変更を戻した後の報告である。現在のVOICEVOXコードや実機の表示は再検証していないため、今後の適用時には対象版の実装と検証経路を読み直す。
今回の確認は、対象ログの該当箇所、既存スキル、共通ルールとの文面照合、公式 `skill-creator` の形式検証までとする。
実際のCodexによる再実装や独立エージェント評価は行っていないため、実装品質の改善を実測したとは扱わない。
