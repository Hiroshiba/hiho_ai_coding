[project]
name = "hiho-ai-coding"
version = "0.1.0"
description = "Claude Code と Codex CLI の設定同期"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tomlkit>=0.13.3",
]

[tool.uv]
package = false
